// @flow

import { createVaultFile, openVaultFile } from './vault'

// TODO: this should connect the store with the vault and other storage options
// the client has to initialise the flow by opening or creating a vault, that will hydrate the store
